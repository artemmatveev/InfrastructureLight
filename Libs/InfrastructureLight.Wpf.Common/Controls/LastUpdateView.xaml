<UserControl x:Class="InfrastructureLight.Wpf.Common.Controls.LastUpdateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InfrastructureLight.Wpf.Common.Controls"
             mc:Ignorable="d">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Cоздал:"/>
            <TextBlock Text="{Binding CreatedBy}"/>
            <TextBlock Text="{Binding CreatedDate, StringFormat=', Дата: dd.MM.yyyy, HH:mm:ss'}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Изменил:"/>
            <TextBlock Text="{Binding ModifyBy}"/>
            <TextBlock Text="{Binding ModifyDate, StringFormat=', Дата: dd.MM.yyyy, HH:mm:ss'}"/>
        </StackPanel>
    </StackPanel>
</UserControl>
